# 🚀 Інструкція використання міграційних скриптів

## Два способи міграції

У вас є **2 скрипти** для міграції проєкту на Next.js 14+:

1. **migrate-to-nextjs.js** (Node.js) - автоматична конвертація коду
2. **migrate-to-nextjs.sh** (Bash) - швидке копіювання структури

---

## 📦 Варіант 1: Node.js скрипт (РЕКОМЕНДОВАНО)

### Переваги:
✅ Автоматична конвертація імпортів  
✅ Додавання 'use client' де потрібно  
✅ Оновлення package.json  
✅ Створення всіх конфігів  
✅ **Копіює ВСІ .tsx і .css файли з папок сторінок**  

### Використання:

```bash
# 1. Експортуйте проєкт з Figma Make
# 2. Розпакуйте ZIP
# 3. Перейдіть у папку проєкту
cd budlider-export

# 4. Скопіюйте скрипт migrate-to-nextjs.js у корінь проєкту

# 5. Запустіть скрипт
node migrate-to-nextjs.js

# 6. Встановіть залежності
npm install

# 7. Запустіть dev сервер
npm run dev
```

### Що робить скрипт:
- ✅ Створює структуру Next.js App Router
- ✅ Конвертує імпорти React Router → Next.js
- ✅ Додає `'use client'` до client components
- ✅ **Копіює ВСІ .tsx та .css файли з кожної папки сторінки**
- ✅ Створює layout.tsx, providers, API routes
- ✅ Копіює компоненти, hooks, stores, types
- ✅ Створює конфігурації (next.config, tsconfig)
- ✅ Оновлює package.json

### 📂 Приклад копіювання файлів:

```
Якщо в src/app/catalog/ є:
├── page.tsx
├── CatalogPage.module.css
├── FilterSidebar.tsx
└── FilterSidebar.module.css

То скрипт скопіює ВСІ ці файли в app/catalog/:
├── page.tsx
├── CatalogPage.module.css
├── FilterSidebar.tsx
└── FilterSidebar.module.css
```

---

## 🐚 Варіант 2: Bash скрипт (ШВИДКИЙ)

### Переваги:
✅ Швидке копіювання без Node.js  
✅ Проста структура  
⚠️ Потребує ручної конвертації імпортів  

### Використання:

```bash
# 1. Експортуйте проєкт з Figma Make
# 2. Розпакуйте ZIP
# 3. Перейдіть у папку проєкту
cd budlider-export

# 4. Скопіюйте скрипт migrate-to-nextjs.sh у корінь проєкту

# 5. Надайте права на виконання
chmod +x migrate-to-nextjs.sh

# 6. Запустіть скрипт
./migrate-to-nextjs.sh

# 7. Оновіть package.json вручну (додайте next, видаліть vite)

# 8. Встановіть залежності
npm install next@latest react@latest react-dom@latest
npm install @types/node @types/react @types/react-dom typescript

# 9. Конвертуйте імпорти вручну (див. README-NEXTJS.md)

# 10. Запустіть dev сервер
npm run dev
```

---

## 📋 Порівняння скриптів

| Особливість | Node.js скрипт | Bash скрипт |
|-------------|----------------|-------------|
| **Конвертація імпортів** | ✅ Автоматично | ❌ Вручну |
| **'use client'** | ✅ Автоматично | ❌ Вручну |
| **Оновлення package.json** | ✅ Автоматично | ❌ Вручну |
| **Швидкість** | Середня | Швидка |
| **Потребує Node.js** | ✅ Так | ❌ Ні |
| **Складність** | Низька | Середня |

---

## 🔧 Після міграції (обидва варіанти)

### 1. Перевірте структуру

```
✅ app/layout.tsx
✅ app/page.tsx
✅ app/catalog/page.tsx
✅ components/Header/
✅ providers/Providers.tsx
✅ styles/globals.css
✅ next.config.mjs
✅ tsconfig.json
```

### 2. Ручна конвертація (якщо використовували Bash скрипт)

**Замініть імпорти в УСІХ файлах:**

```tsx
// ❌ Старе (React Router)
import { Link } from 'react-router-dom';
import { useNavigate } from 'react-router-dom';

const navigate = useNavigate();
navigate('/cart');

<Link to="/catalog">Каталог</Link>

// ✅ Нове (Next.js)
import Link from 'next/link';
import { useRouter } from 'next/navigation';

const router = useRouter();
router.push('/cart');

<Link href="/catalog">Каталог</Link>
```

**Додайте 'use client' до компонентів:**

```tsx
'use client'; // ← Додати на початок файлу

import { useState } from 'react';
// ... решта коду
```

Потребують `'use client'`:
- Компоненти з `useState`, `useEffect`, `useContext`
- Компоненти з event handlers (`onClick`, `onChange`)
- Компоненти що використовують `localStorage`, `window`, `document`

### 3. Оновіть package.json (якщо використовували Bash)

```json
{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "^14.2.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
```

Видаліть:
- `react-router-dom`
- `vite`
- `@vitejs/plugin-react`

### 4. Встановіть залежності

```bash
npm install
```

### 5. Запустіть проєкт

```bash
npm run dev
```

Відкрийте: http://localhost:3000

---

## ❗ Можливі помилки та рішення

### Помилка: "Module not found: Can't resolve 'react-router-dom'"
**Рішення:** Замініть всі імпорти React Router на Next.js (див. вище)

### Помилка: "You're importing a component that needs useState..."
**Рішення:** Додайте `'use client';` на початок файлу

### Помилка: "Cannot find module '@/app/components/...'"
**Рішення:** Замініть `@/app/` на `@/` (оскільки структура змінилась)

```tsx
// ❌ Старе
import { Header } from '@/app/components/Header/Header';

// ✅ Нове
import { Header } from '@/components/Header/Header';
```

### Помилка: "Module not found: Can't resolve '@/app/utils/...'"
**Рішення:** `utils` тепер `lib`

```tsx
// ❌ Старе
import { formatPrice } from '@/app/utils/formatPrice';

// ✅ Нове
import { formatPrice } from '@/lib/formatPrice';
```

---

## 📚 Додаткові ресурси

- [Next.js Docs](https://nextjs.org/docs)
- [App Router Migration Guide](https://nextjs.org/docs/app/building-your-application/upgrading/app-router-migration)
- [Server vs Client Components](https://nextjs.org/docs/app/building-your-application/rendering/composition-patterns)

---

## ✅ Checklist після міграції

- [ ] Структура папок створена
- [ ] Всі сторінки скопійовані
- [ ] Компоненти перенесені
- [ ] Імпорти React Router замінені
- [ ] 'use client' додано де потрібно
- [ ] package.json оновлено
- [ ] Залежності встановлені
- [ ] `npm run dev` запускається без помилок
- [ ] Всі сторінки відкриваються
- [ ] Форми працюють
- [ ] Кошик працює
- [ ] Навігація працює

---

## 🎉 Готово!

Після успішної міграції ваш проєкт працює на **Next.js 14+ App Router** з:
- ✅ TypeScript
- ✅ CSS Modules
- ✅ Server & Client Components
- ✅ API Routes
- ✅ Оптимізованим SEO
- ✅ Швидким рендерингом

Успіхів! 🚀