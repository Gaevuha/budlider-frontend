# 📋 ОСТАТОЧНИЙ ПЛАН РЕСТРУКТУРИЗАЦІЇ ПРОЄКТУ

## ✅ ЩО ЗРОБЛЕНО:
1. Встановлено Zustand для state management
2. Створено базові stores:
   - `/src/store/cartStore.ts`
   - `/src/store/favoritesStore.ts`  
   - `/src/store/authModalStore.ts`
3. Створено структуру модальних вікон:
   - `/src/app/@modal/quick-order/`
4. Частково створено структуру pages:
   - `/src/app/page.tsx` (HomePage)
   - `/src/app/layout.tsx` (RootLayout)

## 🎯 ЩО ЗАЛИШИЛОСЬ ЗРОБИТИ:

### ЕТАП 1: Завершити створення базової структури сторінок
Створити папки і файли для всіх основних роутів:

```
/src/app/
├── page.tsx (HomePage) ✅ ГОТОВО
├── layout.tsx (RootLayout) ✅ ГОТОВО
│
├── catalog/
│   ├── page.tsx
│   └── page.module.css
│
├── product/
│   └── [slug]/
│       ├── page.tsx
│       └── page.module.css
│
├── cart/
│   └── page.tsx
│
├── wishlist/
│   └── page.tsx
│
├── checkout/
│   └── page.tsx
│
├── services/
│   └── page.tsx
│
├── about/
│   └── page.tsx
│
├── delivery/
│   └── page.tsx
│
├── contacts/
│   └── page.tsx
│
├── profile/
│   └── page.tsx
│
└── admin/
    └── orders/
        └── page.tsx
```

### ЕТАП 2: Створити модальні вікна в @modal папці
```
/src/app/@modal/
├── auth/
│   ├── AuthModal.tsx ← ПЕРЕНЕСТИ з /components/AuthModal
│   └── AuthModal.module.css
│
└── quick-order/ ✅ ГОТОВО
    ├── QuickOrderModal.tsx
    └── QuickOrderModal.module.css
```

### ЕТАП 3: Реорганізувати компоненти
Перемістити компоненти у нову структуру `/src/components/`:

```
/src/components/
├── layout/
│   ├── Header/
│   ├── Footer/
│   └── Navigation/
│
├── product/
│   ├── ProductCard/
│   ├── ProductList/
│   └── ProductGallery/
│
├── cart/
│   ├── CartItem/
│   └── CartSummary/
│
├── filters/
│   ├── CatalogFilters/
│   └── FiltersModal/
│
├── forms/
│   └── CheckoutForm/
│
├── common/
│   ├── Hero/
│   ├── Loader/
│   └── Pagination/
│
└── modals/ (експорти)
    ├── index.ts
    └── ModalProvider.tsx
```

### ЕТАП 4: Створити hooks
```
/src/hooks/
├── useCart.ts (wrapper над cartStore)
├── useFavorites.ts (wrapper над favoritesStore)
├── useAuth.ts (wrapper над AuthContext)
└── useProducts.ts (для майбутнього React Query)
```

### ЕТАП 5: Створити types
```
/src/types/
├── product.ts
├── cart.ts
├── order.ts
├── user.ts
└── index.ts (re-exports)
```

### ЕТАП 6: Створити utils та constants
```
/src/utils/
├── formatPrice.ts
├── localStorage.ts
└── toast.ts

/src/constants/
├── routes.ts
└── messages.ts
```

### ЕТАП 7: Оновити основний вхідний файл
Оновити `/src/main.tsx` щоб використовувати новий RootLayout

### ЕТАП 8: Видалити старі файли
Видалити:
- `/src/app/pages/` (вся папка після переносу)
- Старі компоненти модалок з `/src/app/components/`

---

## 🚀 ПОСЛІДОВНІСТЬ ВИКОНАННЯ:

1. **Спочатку:** Створити всі page.tsx файли (без логіки, просто експорти зі старих файлів)
2. **Потім:** Перенести модалки
3. **Потім:** Реорганізувати components
4. **Потім:** Створити hooks, types, utils
5. **Нарешті:** Оновити імпорти і видалити старе

---

## ⚠️ ВАЖЛИВО:
- НЕ ламати існуючий функціонал
- Поступово переносити, тестуючи кожен крок
- Використовувати ре-експорти зі старих файлів на перехідний період

---

**Чи готові почати з ЕТАПУ 1? Я можу створити всі page.tsx файли одним махом.**
